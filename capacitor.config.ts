import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'IonVue 6 Starter',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
